# PR 2 uses only public Binance Futures market-data endpoints.
# Keep real secrets out of git.

CONFIG_PATH=config.yaml

# Reserved for later PRs. Do not commit real values.
BINANCE_API_KEY=
BINANCE_API_SECRET=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
